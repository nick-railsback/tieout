{
  "name": "@tieout/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ponder indexer — SCAFFOLD ONLY for Batch 1. The derivation adapter is Batch 2.",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "build": "ponder codegen",
    "typecheck": "ponder codegen && tsc --noEmit -p tsconfig.json",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.ts"
  },
  "dependencies": {
    "@tieout/addresses": "workspace:*",
    "@tieout/recon": "workspace:*",
    "ponder": "catalog:",
    "hono": "catalog:",
    "viem": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
