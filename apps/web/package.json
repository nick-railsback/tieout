{
  "name": "@tieout/web",
  "version": "0.1.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "The tieout web surface — live wstETH positions (Multicall3 + WebSocket), the explain-itself report diff, and the Base attestation anchor. Renders only real engine/chain output (AD-16/AD-17); it never re-hashes a report (AD-13).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.ts"
  },
  "dependencies": {
    "@tieout/addresses": "workspace:*",
    "@tieout/recon": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vite": "catalog:"
  }
}
